<% if logged_in? %>
<% provide(:title, 'Create event') %>

<div class="row container">
<h1>Create event</h1>
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@event) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>

      <%= f.label :location %>
      <%= f.text_field :location, class: 'form-control' %>

      <%= f.label :description %>
      <%= f.text_area :description, class: 'form-control' %>

      <%= f.label :date_event %>
      <%= f.datetime_field :date_event, class: 'form-control' %>

      <%= f.submit "Create my event", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
<% else %>
<div class="container">
  <h1>Bienvenue !</h1>

  <%= link_to "Créer un compte", new_user_path, class: "btn btn-lg btn-primary" %> </br>
  <%= link_to "Ou connectez-vous", login_path, class: "btn btn-lg btn-primary" %>
</div>
<% end %>